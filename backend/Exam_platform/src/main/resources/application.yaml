server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/exam_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: qxy080810
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.exam.exam_platform.entity

openai:
  api-key: ${OPENAI_API_KEY:}
  responses-url: ${OPENAI_RESPONSES_URL:https://api.openai.com/v1/responses}
  vision-model: ${OPENAI_VISION_MODEL:gpt-4o-mini}
  timeout-seconds: ${OPENAI_TIMEOUT_SECONDS:60}
